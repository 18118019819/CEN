import numpy as np

from rgcn import utils

# snapshots = [1, 2,3,2,4,10,1]
# for snapshot in snapshots:
#     uniq_v, edges = np.unique((snapshot[:, 0], snapshot[:, 2]), return_inverse=True)  # relabel


